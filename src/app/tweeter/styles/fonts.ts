import {Noto_Sans, Poppins} from "next/font/google";

const noto_sans = Noto_Sans({ subsets: ['latin'] });
const poppins = Poppins({ weight: '500', subsets: ['latin'] });

export {noto_sans, poppins}
